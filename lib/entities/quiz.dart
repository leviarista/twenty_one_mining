class Quiz {
  final int level;
  final String scenario;
  final String question;
  final List options;
  final int answer;

  Quiz(this.level, this.scenario, this.question, this.options, this.answer);
}
